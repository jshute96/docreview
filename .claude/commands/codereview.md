Start a code review in a background agent with a clean context. Tell it what the change is supposed to be, like what the commit message would be.

While waiting, do a code review. Check the docs too, and update any docs if necessary.
$ARGUMENTS

Get the output from the review subagent. See if it found anything else we should fix.
